{
  "name": "tdri-subscription-ledger",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test test/dashboard.test.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
